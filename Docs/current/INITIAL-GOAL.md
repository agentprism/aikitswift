This document retains the verbatim user prompts that initiated and steered the implementation for this work.

Initial prompt:
"For @aikitswift/ I want to figure out how to best implement an `Agent` facade that builds on our pi-ai inspired facade that as akin to pi-agent-core's `Agent` facade, if that makes sense"

Steering prompt:

